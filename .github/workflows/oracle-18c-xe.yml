name: Oracle 18c XE

on:
  workflow_dispatch:

jobs:
  oracle-18c-xe:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: true

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and Push
        working-directory: ./oracle-images/OracleDatabase/SingleInstance/dockerfiles
        run: |
          ./buildContainerImage.sh -t dkmoonfruit/oracle:18.4.0-xe -v 18.4.0 -x
          docker push dkmoonfruit/oracle:18.4.0-xe

  oracle-18c-xe-prebuilt:
    runs-on: ubuntu-latest
    needs: oracle-18c-xe
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: true

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Generate initOracle.sh
        working-directory: ./oracle-18c-xe
        run: |
          cp ../oracle-images/OracleDatabase/SingleInstance/dockerfiles/18.4.0/runOracle.sh initOracle.sh
          patch initOracle.sh < initOracle.patch

      - name: Build and Push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          context: ./oracle-18c-xe
          tags: dkmoonfruit/oracle:18.4.0-xe-prebuilt
